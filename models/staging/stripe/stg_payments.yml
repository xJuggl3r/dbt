version: 2

models:
  - name: stg_payments
    description: Staged Stripe payment data
    columns:
      - name: payment_id
        description: The primary key for dim_customers.
        tests:
          - unique
          - not_null