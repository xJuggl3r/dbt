version: 2

models:
  - name: stg_payments
    description: Staged Stripe payment data
    columns:
      - name: id
        description: The primary key for payments.
        tests:
          - unique
          - not_null